export enum ROUTE_PATHS {
  HOME = '/',
  LOGIN = '/login',
  REGISTER = '/register',
  TEST = '/test',
  RESULT = '/test/result'
}

export enum ROUTE_NAMES {
  HOME = 'HOME',
  LOGIN = 'LOGIN',
  REGISTER = 'REGISTER',
  TEST = 'TEST',
  RESULT = 'RESULT'
}
