export enum ROUTE_PATHS {
  HOME = '/',
  LOGIN = '/login',
  REGISTER = '/register',
  FEED = '/feed'
}

export enum ROUTE_NAMES {
  HOME = 'HOME',
  LOGIN = 'LOGIN',
  REGISTER = 'REGISTER',
  FEED = 'FEED'
}
