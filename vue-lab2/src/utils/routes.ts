export enum ROOT_ROUTE_PATHS {
  HOME = '/',
  LOGIN = '/login/',
  POKEMON = '/pokemon/',
  CART = '/cart/'
}

export enum ROOT_ROUTE_NAMES {
  HOME = 'Home',
  LOGIN = 'Login',
  POKEMON = 'Pokemon',
  CART = 'Cart'
}
